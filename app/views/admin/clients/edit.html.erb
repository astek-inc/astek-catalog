<% content_for :header_left do %>
    <%= content_tag(:h1, [link_to('Clients', admin_clients_path), @client.name].join(' / ').html_safe) %>
<% end %>

<%= render partial: 'admin/shared/header' %>

<%= content_tag :div, class: 'col-md-9' do %>
    <%= form_for [:admin, @client] do |f| %>
        <%= field_set_tag nil, 'data-hook': 'client_edit' do %>
            <%= render partial: 'form', locals: { f: f } %>
        <% end %>

        <%= form_buttons admin_clients_path %>
    <% end %>
<% end %>
