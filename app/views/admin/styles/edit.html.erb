<% content_for :header_left do %>
    <%= content_tag(:h1, [link_to('Styles', admin_styles_path), @style.name].join(' / ').html_safe) %>
<% end %>

<%= render partial: 'admin/shared/header' %>

<%= content_tag :div, class: 'col-md-9' do %>
    <%= form_for [:admin, @style] do |f| %>
        <%= field_set_tag nil, 'data-hook': 'style_edit' do %>
            <%= render partial: 'form', locals: { f: f } %>
        <% end %>

        <%= form_buttons admin_styles_path %>
    <% end %>
<% end %>
