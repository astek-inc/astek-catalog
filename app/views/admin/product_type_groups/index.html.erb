<% content_for :header_left do %>
  <%= content_tag :h1, link_to('Product Type Groups', admin_product_type_groups_path) %>
<% end %>

<% content_for :header_right do %>
    <%= link_to 'New Product Type Group', new_admin_product_type_group_path, class: 'btn btn-success' %>
<% end %>

<%= render partial: 'admin/shared/header' %>

<div>

  <% if @product_type_groups.any? %>

      <div class="table-responsive">
        <table class="table table-striped sortable" data-hook data-sortable-link="<%= update_row_order_admin_product_type_groups_path %>" data-position-start="<%= @position_start %>">
          <thead>
          <tr>
            <th class="move-handle"></th>
            <th>Name</th>
            <th>Description</th>
            <th class="actions"></th>
          </tr>
          </thead>

          <tbody>
          <% @product_type_groups.each do |product_type_group| %>
              <tr class="product_property fields" data-item-id=<%= "#{product_type_group.id}" %> data-hook="product_type_group">
                <td class="move-handle text-center">
                  <span class="glyphicon glyphicon-move" aria-hidden="true"></span>
                </td>
                <td><%= product_type_group.name %></td>
                <td><%= truncate(sanitize(product_type_group.description), length: 140) %></td>
                <td class="actions">
                  <div class="btn-group btn-group-sm" role="group" aria-label="Actions">
                    <%= edit_button edit_admin_product_type_group_path product_type_group %>
                    <%= delete_button admin_product_type_group_path product_type_group %>
                  </div>
                </td>
              </tr>
          <% end %>
          </tbody>
        </table>
      </div>

      <%= paginate @product_type_groups %>

  <% else %>
    <p>No product type groups found. <%= link_to 'Create one.', new_admin_product_type_group_path %></p>
  <% end %>

</div>
