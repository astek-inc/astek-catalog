<% content_for :header_left do %>
    <%= content_tag(:h1, [link_to('Properties', admin_properties_path), @property.name].join(' / ').html_safe) %>
<% end %>

<%= render partial: 'admin/shared/header' %>

<%= content_tag :div, class: 'col-md-9' do %>
    <%= form_for [:admin, @property] do |f| %>
        <%= field_set_tag nil, 'data-hook': 'property_edit' do %>
            <%= render partial: 'form', locals: { f: f } %>
        <% end %>

        <%= form_buttons admin_properties_path %>
    <% end %>
<% end %>
